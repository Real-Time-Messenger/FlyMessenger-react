import { ReactNode } from "react";

/**
 * Interface for components that have children.
 *
 * @interface ChildrenProps
 *
 * @property {ReactNode} children - The children of the component.
 */
export interface ChildrenProps {
    children: ReactNode;
}
